name: cloudflare_turnstile
description: A free CAPTCHAs alternative to enhance security and protect against bots.
repository: https://github.com/Nika0000/cloudflare_turnstile
version: 3.2.1

platforms:
  android:
  ios:
  web:

environment:
  sdk: ^3.4.4
  flutter: ">=3.22.3"

dependencies:
  flutter:
    sdk: flutter

  flutter_inappwebview: ^6.2.0-beta.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^6.0.0

topics:
  - hcaptcha
  - recaptcha
  - cloudflare-turnstile